奇偶校验位：在每个数据帧的最后添加一个额外的校验位（称为奇校验位或偶校验位），使得整个帧中“1”的个数（包括数据位和校验位）是奇数或偶数。
奇偶性判断：发送端计算数据位中“1”的个数，如果是奇数，奇校验位设为1；如果是偶数，奇校验位设为0（偶校验相反）。
接收端校验：接收端在接收到数据后，重新计算数据位和校验位中“1”的个数，与发送端的奇偶校验位进行比较。如果不一致，则表明数据在传输过程中发生了错误。
